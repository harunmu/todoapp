export interface Todos{
    id: string;
    text: string;
    category: string;
}

export interface Categories{
    id: string;
    text: string;
}

export interface blogContents{
    id: string;
    date: string;
    title: string;
    text: string;
    tag: string;
}

